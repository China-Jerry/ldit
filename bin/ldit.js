#!/usr/bin/env node
console.log("Let's do it together.");
var cmd = process.argv[2];
if(cmd){
    switch(cmd){
        case "name":
            break;
        case "site":
            break;
        case "email":
            break;
    }
}